---
"seekstone": patch
---

docs: republish the benchmark figures on the fixture-v2 baseline (SHA-322) — 5.2 ms warm keyword search and ~26 ms shipped semantic search at 10k notes, obsidian-tc at ~514× — and check every published number in CI against the committed `benchmarks.json`, generated from the harness baselines.

{
  "$schema": "../src/bench/queries.schema.json",
  "comment": "Version-controlled query set. Edit per-vault so 'rare' actually matches and 'phrase' actually exists. Re-running with the same set is what makes results comparable across snapshots and adapters.",
  "queries": [
    {
      "id": "single-term",
      "kind": "single",
      "query": "engineering",
      "notes": "High-frequency single term across REDACTED, Daily Notes, and Readwise content."
    },
    {
      "id": "multi-term",
      "kind": "multi",
      "query": "team dynamics",
      "notes": "Matches a top tag (#team-dynamics, 112 occurrences); realistic two-word search."
    },
    {
      "id": "phrase",
      "kind": "phrase",
      "query": "\"radical candor\"",
      "notes": "Known book in the vault (Kindle + Readwise copies); confirms phrase search hits and payload size."
    },
    {
      "id": "rare-term",
      "kind": "rare",
      "query": "excalidraw thumbnail",
      "notes": "Low expected hit count; stresses cold-cache behaviour and near-zero payload path."
    }
  ],
  "reads": {
    "small": null,
    "large": null,
    "_hint": "After running `harness profile`, paste the smallest and one of the largest note paths from vault-stats.json here. Leave null to have bench auto-pick from the stats file."
  },
  "runs": 20,
  "_runsHint": "N≥20 per the build brief. Increase for noisier numbers; decrease only when iterating."
}

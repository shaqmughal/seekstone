{
  "$schema": "../src/bench/queries.schema.json",
  "comment": "Version-controlled query set. Edit per-vault so 'rare' actually matches and 'phrase' actually exists. Re-running with the same set is what makes results comparable across snapshots and adapters.",
  "queries": [
    {
      "id": "single-term",
      "kind": "single",
      "query": "leadership",
      "notes": "High-frequency single term — replace with a common word in your vault."
    },
    {
      "id": "multi-term",
      "kind": "multi",
      "query": "team dynamics",
      "notes": "Two-word search — replace with a common multi-word phrase in your vault."
    },
    {
      "id": "phrase",
      "kind": "phrase",
      "query": "deep work",
      "notes": "Known phrase — replace with a quoted phrase you know exists in your vault."
    },
    {
      "id": "rare-term",
      "kind": "rare",
      "query": "market problem discovery",
      "notes": "Low-frequency term — replace with a rare or niche phrase to stress cold-cache and near-zero payload paths."
    }
  ],
  "reads": {
    "small": "Daily Notes/2026-05-17.md",
    "large": "2 Personal/1.2 Projects/Market Problem Discovery/01-inventory.md",
    "_hint": "After running `harness profile`, paste a small (~2 KB) and large (>50 KB) note path from vault-stats.json here. Leave null to auto-pick from the stats file."
  },
  "runs": 20,
  "_runsHint": "N≥20 per the build brief. Increase for noisier numbers; decrease only when iterating."
}

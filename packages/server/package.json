{
  "name": "@seekstone/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Seekstone MCP server — filesystem-direct Obsidian vault access, low context-tax.",
  "bin": {
    "seekstone": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@seekstone/core": "*",
    "minisearch": "^7.1.1",
    "yaml": "^2.6.1",
    "zod": "^3.24.0"
  }
}
